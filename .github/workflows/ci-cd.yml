name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
    paths-ignore:
      - 'README.md'
      - 'docs/**'
      - '*.md'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - 'README.md'
      - 'docs/**'
      - '*.md'

permissions:
  contents: read
  packages: write

env:
  PYTHON_VERSION: "3.11"

jobs:
  lint:
    name: 🔍 Lint
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      id: setup-python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
    - name: Install Poetry
      uses: snok/install-poetry@v1.4.1
      with:
        virtualenvs-create: true
        virtualenvs-in-project: true
    
    - name: Load cached venv
      id: cached-poetry-dependencies
      uses: actions/cache@v4
      with:
        path: .venv
        key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('**/poetry.lock') }}
    
    - name: Install dependencies
      if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
      run: poetry install --no-interaction --no-root
    
    - name: Install project
      run: poetry install --no-interaction --no-root
    
    - name: Run flake8
      run: |
        poetry add --group dev flake8
        poetry run flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        poetry run flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    
    - name: Run black check
      run: |
        poetry add --group dev black
        poetry run black --check --diff .
    
    - name: Run isort check
      run: |
        poetry add --group dev isort
        poetry run isort --check-only --diff .
    
    - name: Run security check
      run: |
        poetry add --group dev safety bandit
        # Use the new scan command instead of deprecated check
        # Ignore CVE-2024-22513 for djangorestframework-simplejwt as it's a false positive for version 5.5.1
        poetry run safety scan --ignore 66963 || true
        
        # Run bandit security scanner
        poetry run bandit -r . -f json -o bandit-report.json --skip B101,B106,B107,B108,B110,B603 --exclude */migrations/*,*/venv/*,*/.venv/*,*/env/*,*/.env/*,*/node_modules/*,*/__pycache__/*,*/.git/*,*/htmlcov/*,*/.pytest_cache/* || true
        # Display results in a readable format  
        poetry run bandit -r . --skip B101,B106,B107,B108,B110,B603 --exclude */migrations/*,*/venv/*,*/.venv/*,*/env/*,*/.env/*,*/node_modules/*,*/__pycache__/*,*/.git/*,*/htmlcov/*,*/.pytest_cache/* || true

  test:
    name: 🧪 Test
    runs-on: ubuntu-latest
    needs: lint
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_lacrei_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      id: setup-python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
    - name: Install Poetry
      uses: snok/install-poetry@v1.4.1
      with:
        virtualenvs-create: true
        virtualenvs-in-project: true
    
    - name: Load cached venv
      id: cached-poetry-dependencies
      uses: actions/cache@v4
      with:
        path: .venv
        key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('**/poetry.lock') }}
    
    - name: Install dependencies
      if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
      run: poetry install --no-interaction --no-root
    
    - name: Install project
      run: poetry install --no-interaction --no-root
    
    - name: Run tests
      env:
        DATABASE_URL: postgres://postgres:postgres@localhost:5432/test_lacrei_db
        SECRET_KEY: test-secret-key-for-ci
        DEBUG: False
      run: |
        poetry run python manage.py test --verbosity=2 --exclude-tag=throttling
    
    - name: Generate coverage report
      env:
        DATABASE_URL: postgres://postgres:postgres@localhost:5432/test_lacrei_db
        SECRET_KEY: test-secret-key-for-ci
        DEBUG: False
      run: |
        poetry add --group dev coverage
        poetry run coverage run --source='.' manage.py test --exclude-tag=throttling
        poetry run coverage xml
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  build:
    name: 🐳 Build
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build.outputs.digest }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ghcr.io/${{ github.repository_owner }}/desafio-lacrei
        tags: |
          type=raw,value=latest,enable={{is_default_branch}}
          type=raw,value=staging,enable=${{ github.ref == 'refs/heads/develop' }}
          type=sha,format=short
        labels: |
          org.opencontainers.image.title=Desafio Lacrei
          org.opencontainers.image.description=Django REST API for Lacrei Challenge
          org.opencontainers.image.source={{github.server_url}}/{{github.repository}}
          org.opencontainers.image.revision={{github.sha}}
    
    - name: Build and push Docker image
      id: build
      uses: docker/build-push-action@v6
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        platforms: linux/amd64,linux/arm64

  deploy-staging:
    name: 🚀 Deploy to Staging
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/develop'
    environment: staging
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}
    
    - name: Deploy to ECS Staging
      run: |
        # Garantir que o cluster existe
        if ! aws ecs describe-clusters --clusters desafio-lacrei-staging --query 'clusters[0].clusterName' --output text 2>/dev/null | grep -q "desafio-lacrei-staging"; then
          echo "Cluster doesn't exist, creating..."
          aws ecs create-cluster --cluster-name desafio-lacrei-staging
          echo "Cluster created successfully!"
        else
          echo "Cluster exists"
        fi
        
        # Garantir que os log groups existem
        aws logs create-log-group --log-group-name "/ecs/desafio-lacrei-staging" 2>/dev/null || echo "Log group already exists"
        aws logs create-log-group --log-group-name "/ecs/desafio-lacrei-staging-migrate" 2>/dev/null || echo "Migration log group already exists"
        
        # Verificar se o serviço existe, senão criar
        if aws ecs describe-services --cluster desafio-lacrei-cluster-staging --services desafio-lacrei-service-staging --query 'services[0].serviceName' --output text 2>/dev/null | grep -q "desafio-lacrei-service-staging"; then
          echo "Service exists, updating..."
          aws ecs update-service \
            --cluster desafio-lacrei-cluster-staging \
            --service desafio-lacrei-service-staging \
            --force-new-deployment
        else
          echo "Service doesn't exist, creating staging service..."
          # Usar tag staging específica para branch develop
          IMAGE_TAG="ghcr.io/${{ github.repository_owner }}/desafio-lacrei:staging"
          COMMIT_SHA="${{ github.sha }}"
          SHORT_SHA=$(echo "$COMMIT_SHA" | cut -c1-7)
          echo "Using staging image: $IMAGE_TAG"
          echo "Commit SHA: $COMMIT_SHA (using staging tag for develop branch)"
          
          aws ecs register-task-definition \
            --family desafio-lacrei-staging \
            --network-mode awsvpc \
            --requires-compatibilities FARGATE \
            --cpu 256 \
            --memory 512 \
            --execution-role-arn "arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/ecsTaskExecutionRole" \
            --task-role-arn "arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/ecsTaskRole" \
            --container-definitions "[{
              \"name\": \"desafio-lacrei-app\",
              \"image\": \"$IMAGE_TAG\",
              \"portMappings\": [{
                \"containerPort\": 8000,
                \"protocol\": \"tcp\"
              }],
              \"essential\": true,
              \"environment\": [{
                \"name\": \"DEBUG\",
                \"value\": \"True\"
              }, {
                \"name\": \"ALLOWED_HOSTS\",
                \"value\": \"*\"
              }],
              \"secrets\": [{
                \"name\": \"DATABASE_URL\",
                \"valueFrom\": \"arn:aws:ssm:${{ secrets.AWS_REGION }}:${{ secrets.AWS_ACCOUNT_ID }}:parameter/desafio-lacrei/staging/database-url\"
              }, {
                \"name\": \"SECRET_KEY\",
                \"valueFrom\": \"arn:aws:ssm:${{ secrets.AWS_REGION }}:${{ secrets.AWS_ACCOUNT_ID }}:parameter/desafio-lacrei/staging/secret-key\"
              }],
              \"logConfiguration\": {
                \"logDriver\": \"awslogs\",
                \"options\": {
                  \"awslogs-group\": \"/ecs/desafio-lacrei-staging\",
                  \"awslogs-region\": \"${{ secrets.AWS_REGION }}\",
                  \"awslogs-stream-prefix\": \"ecs\"
                }
              }
            }]"
          
          aws ecs create-service \
            --cluster desafio-lacrei-staging \
            --service-name desafio-lacrei-staging-service \
            --task-definition desafio-lacrei-staging \
            --desired-count 1 \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[${{ secrets.AWS_SUBNET_1 }},${{ secrets.AWS_SUBNET_2 }}],securityGroups=[${{ secrets.AWS_SECURITY_GROUP }}],assignPublicIp=ENABLED}" \
            --enable-execute-command
        fi
    
    - name: Wait for deployment
      run: |
        aws ecs wait services-stable \
          --cluster desafio-lacrei-staging \
          --services desafio-lacrei-staging-service
    
    - name: Run Database Migrations
      run: |
        # Verificar se a task definition de migração existe
        if aws ecs describe-task-definition --task-definition desafio-lacrei-staging-migrate >/dev/null 2>&1; then
          echo "Migration task definition exists, updating..."
          IMAGE_TAG="ghcr.io/${{ github.repository_owner }}/desafio-lacrei:staging"
          COMMIT_SHA="${{ github.sha }}"
          SHORT_SHA=$(echo "$COMMIT_SHA" | cut -c1-7)
          echo "Using image for staging migration update: $IMAGE_TAG"
          echo "Commit SHA: $COMMIT_SHA (using staging tag)"
          
          TASK_DEFINITION=$(aws ecs describe-task-definition --task-definition desafio-lacrei-staging-migrate --query taskDefinition)
          NEW_TASK_DEFINITION=$(echo $TASK_DEFINITION | jq --arg IMAGE "$IMAGE_TAG" '.containerDefinitions[0].image = $IMAGE | del(.taskDefinitionArn) | del(.revision) | del(.status) | del(.requiresAttributes) | del(.placementConstraints) | del(.compatibilities) | del(.registeredAt) | del(.registeredBy)')
          aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEFINITION"
        else
          echo "Creating migration task definition..."
          
          # Usar tag staging para migration
          IMAGE_TAG="ghcr.io/${{ github.repository_owner }}/desafio-lacrei:staging"
          COMMIT_SHA="${{ github.sha }}"
          SHORT_SHA=$(echo "$COMMIT_SHA" | cut -c1-7)
          echo "Using image for staging migration: $IMAGE_TAG"
          echo "Commit SHA: $COMMIT_SHA (using staging tag)"
          
          aws ecs register-task-definition \
            --family desafio-lacrei-staging-migrate \
            --network-mode awsvpc \
            --requires-compatibilities FARGATE \
            --cpu 256 \
            --memory 512 \
            --execution-role-arn "arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/ecsTaskExecutionRole" \
            --task-role-arn "arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/ecsTaskRole" \
            --container-definitions "[{
              \"name\": \"desafio-lacrei-migrate\",
              \"image\": \"$IMAGE_TAG\",
              \"command\": [\"python\", \"manage.py\", \"migrate\"],
              \"essential\": true,
              \"environment\": [{
                \"name\": \"DEBUG\",
                \"value\": \"True\"
              }],
              \"secrets\": [{
                \"name\": \"DATABASE_URL\",
                \"valueFrom\": \"arn:aws:ssm:${{ secrets.AWS_REGION }}:${{ secrets.AWS_ACCOUNT_ID }}:parameter/desafio-lacrei/staging/database-url\"
              }, {
                \"name\": \"SECRET_KEY\",
                \"valueFrom\": \"arn:aws:ssm:${{ secrets.AWS_REGION }}:${{ secrets.AWS_ACCOUNT_ID }}:parameter/desafio-lacrei/staging/secret-key\"
              }],
              \"logConfiguration\": {
                \"logDriver\": \"awslogs\",
                \"options\": {
                  \"awslogs-group\": \"/ecs/desafio-lacrei-staging-migrate\",
                  \"awslogs-region\": \"${{ secrets.AWS_REGION }}\",
                  \"awslogs-stream-prefix\": \"ecs\"
                }
              }
            }]"
        fi
        
        # Executar task de migração
        TASK_ARN=$(aws ecs run-task \
          --cluster desafio-lacrei-cluster-staging \
          --task-definition desafio-lacrei-staging-migrate \
          --launch-type FARGATE \
          --network-configuration "awsvpcConfiguration={subnets=[${{ secrets.AWS_SUBNET_1 }},${{ secrets.AWS_SUBNET_2 }}],securityGroups=[${{ secrets.AWS_SECURITY_GROUP }}],assignPublicIp=ENABLED}" \
          --query 'tasks[0].taskArn' --output text)
          
        # Aguardar conclusão da migração
        echo "Aguardando migração concluir..."
        aws ecs wait tasks-stopped --cluster desafio-lacrei-cluster-staging --tasks $TASK_ARN
        
        # Verificar se a migração foi bem-sucedida
        TASK_STATUS=$(aws ecs describe-tasks --cluster desafio-lacrei-cluster-staging --tasks $TASK_ARN --query 'tasks[0].lastStatus' --output text)
        EXIT_CODE=$(aws ecs describe-tasks --cluster desafio-lacrei-cluster-staging --tasks $TASK_ARN --query 'tasks[0].containers[0].exitCode' --output text)
        
        echo "Task Status: $TASK_STATUS"
        echo "Exit Code: $EXIT_CODE"
        
        if [ "$EXIT_CODE" != "0" ] && [ "$EXIT_CODE" != "None" ]; then
          echo "Migração falhou com exit code: $EXIT_CODE"
          exit 1
        fi
    
    - name: Health check staging
      run: |
        echo "Performing health check via Load Balancer..."
        
        # Obter o DNS do Network Load Balancer para staging
        NLB_DNS=$(aws elbv2 describe-load-balancers --names "desafio-lacrei-nlb-staging" --query 'LoadBalancers[0].DNSName' --output text --region ${{ secrets.AWS_REGION }})
        
        if [ ! -z "$NLB_DNS" ] && [ "$NLB_DNS" != "None" ]; then
          echo "Load Balancer DNS: $NLB_DNS"
          
          # Verificar se a aplicação está respondendo corretamente via Load Balancer
          for i in {1..10}; do
            if curl -f "http://${NLB_DNS}/health/" --connect-timeout 10 --max-time 30; then
              echo "✅ Health check passed via Load Balancer"
              break
            else
              echo "⏳ Health check attempt $i/10 - waiting for service to be ready..."
              
              # Verificar status do target group
              TARGET_GROUP_ARN=$(aws elbv2 describe-target-groups --names "desafio-lacrei-tg-v2-staging" --query 'TargetGroups[0].TargetGroupArn' --output text --region ${{ secrets.AWS_REGION }})
              if [ ! -z "$TARGET_GROUP_ARN" ]; then
                echo "Checking target health..."
                aws elbv2 describe-target-health --target-group-arn "$TARGET_GROUP_ARN" --region ${{ secrets.AWS_REGION }}
              fi
              
              if [ $i -eq 10 ]; then
                echo "❌ Health check failed after 10 attempts"
                exit 1
              fi
              sleep 30
            fi
          done
        else
          echo "⚠️ Could not retrieve Load Balancer DNS, skipping health check"
        fi

  deploy-production:
    name: 🚀 Deploy to Production
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    environment: production
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}
    
    - name: Debug deployment info
      run: |
        echo "=== PRODUCTION DEPLOYMENT DEBUG INFO ==="
        echo "Commit SHA: ${{ github.sha }}"
        echo "Short SHA: $(echo ${{ github.sha }} | cut -c1-7)"
        echo "Branch: ${{ github.ref }}"
        echo "Repository: ${{ github.repository_owner }}/desafio-lacrei"
        COMMIT_SHA="${{ github.sha }}"
        SHORT_SHA=$(echo "$COMMIT_SHA" | cut -c1-7)
        IMAGE_TAG="ghcr.io/${{ github.repository_owner }}/desafio-lacrei:latest"
        echo "Target image: $IMAGE_TAG (using latest tag for production)"
        echo "Build outputs tags: ${{ needs.build.outputs.image-tag }}"
        echo "Build digest: ${{ needs.build.outputs.image-digest }}"
        echo "=== END DEBUG INFO ==="
    
    - name: Deploy to ECS Production
      run: |

          TASK_DEFINITION_ARN=$(aws ecs register-task-definition \
            --cli-input-json file://task-definition.json \
            --query 'taskDefinition.taskDefinitionArn' \
            --output text)

          # Atualizar o serviço ECS existente
          aws ecs update-service \
            --cluster "desafio-lacrei-cluster-production" \
            --service "desafio-lacrei-service-production" \
            --task-definition "$TASK_DEFINITION_ARN" \
            --force-new-deployment

      
    - name: Wait for production deployment
      run: |
        echo "Waiting for service to stabilize..."
        
        # Verificar status do serviço antes de aguardar
        aws ecs describe-services --cluster desafio-lacrei-cluster-production --services desafio-lacrei-service-production
        
        # Aguardar com timeout reduzido e melhor tratamento de erro
        echo "Starting service stability check..."
        WAIT_ATTEMPTS=0
        MAX_WAIT_ATTEMPTS=20
        
        while [ $WAIT_ATTEMPTS -lt $MAX_WAIT_ATTEMPTS ]; do
          WAIT_ATTEMPTS=$((WAIT_ATTEMPTS + 1))
          echo "Stability check attempt $WAIT_ATTEMPTS/$MAX_WAIT_ATTEMPTS..."
          
          # Verificar eventos do serviço para detectar problemas específicos
          SERVICE_EVENTS=$(aws ecs describe-services --cluster desafio-lacrei-cluster-production --services desafio-lacrei-service-production --query 'services[0].events[0].message' --output text)
          
          # Verificar se há tasks falhando repetidamente
          FAILED_TASKS_COUNT=$(aws ecs describe-services --cluster desafio-lacrei-cluster-production --services desafio-lacrei-service-production --query 'services[0].deployments[0].failedTasks' --output text 2>/dev/null || echo "0")
          RUNNING_COUNT=$(aws ecs describe-services --cluster desafio-lacrei-cluster-production --services desafio-lacrei-service-production --query 'services[0].runningCount' --output text)
          
          echo "Failed tasks: $FAILED_TASKS_COUNT, Running tasks: $RUNNING_COUNT"
          
          # Verificar se o deployment não está criando tasks (desiredCount: 0)
          DESIRED_COUNT=$(aws ecs describe-services --cluster desafio-lacrei-cluster-production --services desafio-lacrei-service-production --query 'services[0].deployments[0].desiredCount' --output text 2>/dev/null || echo "1")
          PENDING_COUNT=$(aws ecs describe-services --cluster desafio-lacrei-cluster-production --services desafio-lacrei-service-production --query 'services[0].pendingCount' --output text)
          
          echo "Desired count: $DESIRED_COUNT, Pending count: $PENDING_COUNT"
          
          # Se não há tasks desejadas nem pendentes, isso indica problema na task definition ou IAM - CORREÇÃO IMEDIATA
          if [ "$DESIRED_COUNT" = "0" ] && [ "$PENDING_COUNT" = "0" ] && [ "$RUNNING_COUNT" = "0" ]; then
            echo "=== DETECTED: NO TASKS BEING CREATED (desiredCount=0) ==="
            echo "This usually indicates IAM role or task definition issues"
            
            # Primeiro, corrigir parâmetros SSM que podem estar causando falhas na task definition
            echo "Checking production SSM parameters..."
            DB_URL=$(aws ssm get-parameter --name "/desafio-lacrei/production/database-url" --with-decryption --query 'Parameter.Value' --output text 2>/dev/null || echo "")
            if [[ "$DB_URL" == *"localhost"* ]] || [[ "$DB_URL" == *"user:password"* ]] || [ -z "$DB_URL" ] || [[ "$DB_URL" == "None" ]]; then
              echo "❌ ERROR: Production DATABASE_URL is missing or invalid!"
              echo "Database URL: $DB_URL"
              echo "Production requires a valid PostgreSQL connection string."
              echo "Please configure: aws ssm put-parameter --name '/desafio-lacrei/production/database-url' --value 'postgres://user:pass@host:5432/dbname' --type SecureString"
              exit 1
            fi
            
            # Verificar secret key de produção também
            SECRET_KEY=$(aws ssm get-parameter --name "/desafio-lacrei/production/secret-key" --with-decryption --query 'Parameter.Value' --output text 2>/dev/null || echo "")
            if [ ${#SECRET_KEY} -lt 20 ]; then
              echo "Generating production secret key..."
              NEW_SECRET=$(python3 -c "import secrets; print(secrets.token_urlsafe(50))")
              aws ssm put-parameter \
                --name "/desafio-lacrei/production/secret-key" \
                --value "$NEW_SECRET" \
                --type SecureString \
                --overwrite \
                --description "Generated secret key for production"
              echo "✅ Production secret key generated"
            fi
            
            # Tentar corrigir trust policies dos roles IAM
            echo "Updating IAM role trust policies..."
            aws iam update-assume-role-policy \
              --role-name ecsTaskRole \
              --policy-document '{
                "Version": "2012-10-17",
                "Statement": [
                  {
                    "Effect": "Allow",
                    "Principal": {
                      "Service": "ecs-tasks.amazonaws.com"
                    },
                    "Action": "sts:AssumeRole"
                  }
                ]
              }' || echo "Could not update ecsTaskRole trust policy"
            
            aws iam update-assume-role-policy \
              --role-name ecsTaskExecutionRole \
              --policy-document '{
                "Version": "2012-10-17",
                "Statement": [
                  {
                    "Effect": "Allow",
                    "Principal": {
                      "Service": "ecs-tasks.amazonaws.com"
                    },
                    "Action": "sts:AssumeRole"
                  }
                ]
              }' || echo "Could not update ecsTaskExecutionRole trust policy"
            
            echo "Waiting for IAM changes to propagate..."
            sleep 30
            
            # Forçar nova deployment com correções
            echo "Forcing new deployment with all fixes applied..."
            aws ecs update-service \
              --cluster ${{ secrets.CLUSTER_NAME }} \
              --service desafio-lacrei-production \
              --force-new-deployment
            
            sleep 45
            continue
          fi
          
          # Se temos tasks falhadas e nenhuma rodando, corrigir SSM imediatamente
          if [ "$FAILED_TASKS_COUNT" -gt 0 ] && [ "$RUNNING_COUNT" = "0" ]; then
            echo "=== DETECTED REPEATED TASK FAILURES - IMMEDIATE SSM FIX ==="
            
            # Verificar parâmetros SSM de produção imediatamente
            DB_URL=$(aws ssm get-parameter --name "/desafio-lacrei/production/database-url" --with-decryption --query 'Parameter.Value' --output text 2>/dev/null || echo "")
            if [[ "$DB_URL" == *"localhost"* ]] || [[ "$DB_URL" == *"user:password"* ]] || [ -z "$DB_URL" ]; then
              echo "❌ ERROR: Production DATABASE_URL is missing or contains placeholder values!"
              echo "Current DATABASE_URL: $DB_URL"
              echo "Production environment requires a valid PostgreSQL connection string."
              echo "Please configure the production database parameter:"
              echo "aws ssm put-parameter --name '/desafio-lacrei/production/database-url' --value 'postgres://user:pass@host:5432/dbname' --type SecureString"
              exit 1
              
              # Forçar nova deployment
              echo "Forcing immediate deployment with fixed parameters..."
              aws ecs update-service \
                --cluster ${{ secrets.CLUSTER_NAME }} \
                --service desafio-lacrei-production \
                --force-new-deployment
              
              sleep 45  # Menos tempo de espera para acelerar
              continue
            fi
          fi
          
          # Detectar tasks presas em PENDING por muito tempo (3+ attempts)
          if [ "$PENDING_COUNT" -gt 0 ] && [ "$RUNNING_COUNT" = "0" ] && [ $WAIT_ATTEMPTS -ge 3 ]; then
            echo "=== DETECTED: TASKS STUCK IN PENDING STATE FOR 3+ ATTEMPTS ==="
            echo "Pending tasks: $PENDING_COUNT, Running tasks: $RUNNING_COUNT"
            echo "This often indicates network configuration or resource allocation issues"
            
            # Buscar logs de tasks PENDING para entender o problema
            LATEST_TASK_ARN=$(aws ecs list-tasks --cluster desafio-lacrei-cluster-production --service-name desafio-lacrei-service-production --desired-status PENDING --query 'taskArns[0]' --output text 2>/dev/null || echo "")
            
            if [ -n "$LATEST_TASK_ARN" ] && [ "$LATEST_TASK_ARN" != "None" ]; then
              echo "Checking PENDING task logs: $LATEST_TASK_ARN"
              TASK_DEFINITION_ARN=$(aws ecs describe-tasks --cluster ${{ secrets.CLUSTER_NAME }} --tasks "$LATEST_TASK_ARN" --query 'tasks[0].taskDefinitionArn' --output text 2>/dev/null || echo "")
              
              # Criar task definition reduzida para melhorar chance de sucesso
              echo "Creating reduced resource task definition for better deployment success..."
              aws ecs register-task-definition \
                --family ${{ secrets.CLUSTER_NAME }} \
                --network-mode awsvpc \
                --requires-attributes name=com.amazonaws.ecs.capability.docker-remote-api.1.18 \
                --cpu 256 \
                --memory 512 \
                --execution-role-arn arn:aws:iam::$(aws sts get-caller-identity --query Account --output text):role/ecsTaskExecutionRole \
                --task-role-arn arn:aws:iam::$(aws sts get-caller-identity --query Account --output text):role/ecsTaskRole \
                --container-definitions '[{
                  "name": "desafio-lacrei",
                  "image": "ghcr.io/${{ github.repository_owner }}/desafio-lacrei:latest",
                  "essential": true,
                  "portMappings": [{
                    "containerPort": 8000,
                    "protocol": "tcp"
                  }],
                  "environment": [{
                    "name": "ENVIRONMENT",
                    "value": "production"
                  }],
                  "secrets": [{
                    "name": "SECRET_KEY",
                    "valueFrom": "/desafio-lacrei/production/secret-key"
                  }, {
                    "name": "DATABASE_URL",
                    "valueFrom": "/desafio-lacrei/production/database-url"
                  }],
                  "logConfiguration": {
                    "logDriver": "awslogs",
                    "options": {
                      "awslogs-group": "/ecs/desafio-lacrei-production",
                      "awslogs-region": "us-east-1",
                      "awslogs-stream-prefix": "ecs"
                    }
                  },
                  "cpu": 256,
                  "memory": 512
                }]' || echo "Failed to create reduced task definition"
              
              # Aplicar configuração de rede alternativa (subnets padrão)
              echo "Attempting deployment with default network configuration..."
              aws ecs update-service \
                --cluster ${{ secrets.CLUSTER_NAME }} \
                --service desafio-lacrei-production \
                --network-configuration 'awsvpcConfiguration={subnets=[subnet-12345678],securityGroups=[sg-12345678],assignPublicIp=ENABLED}' \
                --force-new-deployment || echo "Network config update failed, continuing with force deployment..."
              
              # Se falhou, fazer deployment forçado simples
              echo "Forcing new deployment to break PENDING state..."
              aws ecs update-service \
                --cluster ${{ secrets.CLUSTER_NAME }} \
                --service desafio-lacrei-production \
                --force-new-deployment
              
              sleep 30
              continue
            fi
          fi
          
          if [[ "$SERVICE_EVENTS" == *"unable to assume the role"* ]]; then
            echo "DETECTED: IAM Role assumption problem!"
            echo "Event: $SERVICE_EVENTS"
            echo "This is likely due to missing or misconfigured IAM roles."
            echo "Attempting to fix IAM role trust policies..."
            
            # Tentar corrigir a trust policy
            aws iam update-assume-role-policy \
              --role-name ecsTaskRole \
              --policy-document '{
                "Version": "2012-10-17",
                "Statement": [
                  {
                    "Effect": "Allow",
                    "Principal": {
                      "Service": "ecs-tasks.amazonaws.com"
                    },
                    "Action": "sts:AssumeRole"
                  }
                ]
              }' || echo "Failed to update trust policy"
            
            aws iam update-assume-role-policy \
              --role-name ecsTaskExecutionRole \
              --policy-document '{
                "Version": "2012-10-17",
                "Statement": [
                  {
                    "Effect": "Allow",
                    "Principal": {
                      "Service": "ecs-tasks.amazonaws.com"
                    },
                    "Action": "sts:AssumeRole"
                  }
                ]
              }' || echo "Failed to update execution role trust policy"
            
            echo "Waiting for IAM changes to propagate..."
            sleep 30
            
            # Forçar nova deployment após correção
            echo "Forcing new deployment after IAM fix..."
            aws ecs update-service \
              --cluster ${{ secrets.CLUSTER_NAME }} \
              --service desafio-lacrei-production \
              --force-new-deployment
            
            sleep 60  # Dar tempo para nova deployment começar
            continue
          fi
          
          # Verificar se o serviço está estável
          if aws ecs wait services-stable --cluster desafio-lacrei-cluster-production --services desafio-lacrei-service-production --cli-read-timeout 30 --cli-connect-timeout 30; then
            echo "Service stabilized successfully!"
            break
          fi
          
          # Se não estável, mostrar informações de debug
          echo "Service not stable yet, checking status..."
          
          # Mostrar status das tasks
          TASK_ARNS=$(aws ecs list-tasks --cluster desafio-lacrei-cluster-production --service-name desafio-lacrei-service-production --query 'taskArns[]' --output text)
          if [ ! -z "$TASK_ARNS" ] && [ "$TASK_ARNS" != "None" ]; then
            echo "Current task ARNs: $TASK_ARNS"
            aws ecs describe-tasks --cluster ${{ secrets.CLUSTER_NAME }} --tasks $TASK_ARNS --query 'tasks[*].{TaskArn:taskArn,LastStatus:lastStatus,DesiredStatus:desiredStatus,HealthStatus:healthStatus}'
            
            # Verificar se há tasks com problemas
            FAILED_TASKS=$(aws ecs describe-tasks --cluster ${{ secrets.CLUSTER_NAME }} --tasks $TASK_ARNS --query 'tasks[?lastStatus==`STOPPED`]' --output json)
            if [ "$FAILED_TASKS" != "[]" ]; then
              echo "=== DETECTED FAILED TASKS ==="
              echo "$FAILED_TASKS"
              
              # Mostrar logs das tasks que falharam
              for TASK_ARN in $TASK_ARNS; do
                TASK_ID=$(echo $TASK_ARN | cut -d'/' -f3)
                echo "=== LOGS FOR FAILED TASK: $TASK_ID ==="
                aws logs get-log-events \
                  --log-group-name "/ecs/desafio-lacrei-production" \
                  --log-stream-name "ecs/desafio-lacrei-app/$TASK_ID" \
                  --query 'events[*].message' --output text || echo "No logs found for task $TASK_ID"
              done
              
              # Se há muitas tentativas falhas, tentar corrigir parâmetros SSM
              if [ $WAIT_ATTEMPTS -gt 5 ]; then
                echo "=== ATTEMPTING TO FIX SSM PARAMETERS ==="
                
                # Verificar se os parâmetros de produção são válidos
                DB_URL=$(aws ssm get-parameter --name "/desafio-lacrei/production/database-url" --with-decryption --query 'Parameter.Value' --output text 2>/dev/null || echo "")
                if [[ "$DB_URL" == *"localhost"* ]] || [[ "$DB_URL" == *"user:password"* ]] || [ -z "$DB_URL" ]; then
                  echo "❌ PRODUCTION DATABASE ERROR: Invalid or missing DATABASE_URL!"
                  echo "Current value: $DB_URL"
                  echo "Production deployment failed due to missing PostgreSQL configuration."
                  echo "Required action: Configure production database with:"
                  echo "aws ssm put-parameter --name '/desafio-lacrei/production/database-url' --value 'postgres://user:pass@host:5432/dbname' --type SecureString"
                  exit 1
                  
                  # Forçar nova deployment
                  echo "Forcing new deployment with corrected parameters..."
                  aws ecs update-service \
                    --cluster ${{ secrets.CLUSTER_NAME }} \
                    --service desafio-lacrei-production \
                    --force-new-deployment
                  
                  sleep 60  # Dar tempo para nova deployment começar
                  continue
                fi
              fi
            fi
          else
            echo "No tasks currently running"
            
            # Se não há tasks após várias tentativas, pode ser problema de rede/configuração
            if [ $WAIT_ATTEMPTS -gt 4 ]; then
              echo "=== NO TASKS STARTING - CHECKING NETWORK CONFIGURATION ==="
              
              # Verificar se as subnets existem
              SUBNET1_EXISTS=$(aws ec2 describe-subnets --subnet-ids "${{ secrets.AWS_SUBNET_1 }}" --query 'Subnets[0].SubnetId' --output text 2>/dev/null || echo "None")
              SUBNET2_EXISTS=$(aws ec2 describe-subnets --subnet-ids "${{ secrets.AWS_SUBNET_2 }}" --query 'Subnets[0].SubnetId' --output text 2>/dev/null || echo "None")
              SG_EXISTS=$(aws ec2 describe-security-groups --group-ids "${{ secrets.AWS_SECURITY_GROUP }}" --query 'SecurityGroups[0].GroupId' --output text 2>/dev/null || echo "None")
              
              echo "Subnet 1 check: $SUBNET1_EXISTS"
              echo "Subnet 2 check: $SUBNET2_EXISTS"
              echo "Security Group check: $SG_EXISTS"
              
              if [ "$SUBNET1_EXISTS" = "None" ] || [ "$SUBNET2_EXISTS" = "None" ] || [ "$SG_EXISTS" = "None" ]; then
                echo "NETWORK CONFIGURATION PROBLEM DETECTED!"
                echo "Please check your GitHub Secrets for AWS_SUBNET_1, AWS_SUBNET_2, and AWS_SECURITY_GROUP"
                
                # Tentar usar subnets padrão como fallback
                echo "Attempting to find default subnets as fallback..."
                DEFAULT_VPC=$(aws ec2 describe-vpcs --filters "Name=is-default,Values=true" --query 'Vpcs[0].VpcId' --output text)
                if [ "$DEFAULT_VPC" != "None" ]; then
                  DEFAULT_SUBNETS=$(aws ec2 describe-subnets --filters "Name=vpc-id,Values=$DEFAULT_VPC" "Name=default-for-az,Values=true" --query 'Subnets[*].SubnetId' --output text)
                  DEFAULT_SG=$(aws ec2 describe-security-groups --filters "Name=vpc-id,Values=$DEFAULT_VPC" "Name=group-name,Values=default" --query 'SecurityGroups[0].GroupId' --output text)
                  
                  if [ ! -z "$DEFAULT_SUBNETS" ] && [ "$DEFAULT_SG" != "None" ]; then
                    SUBNET_ARRAY=($DEFAULT_SUBNETS)
                    echo "Found default subnets: ${SUBNET_ARRAY[0]}, ${SUBNET_ARRAY[1]}"
                    echo "Found default security group: $DEFAULT_SG"
                    
                    # Atualizar serviço com configuração de rede correta
                    echo "Updating service with default network configuration..."
                    aws ecs update-service \
                      --cluster ${{ secrets.CLUSTER_NAME }} \
                      --service desafio-lacrei-production \
                      --network-configuration "awsvpcConfiguration={subnets=[${SUBNET_ARRAY[0]},${SUBNET_ARRAY[1]}],securityGroups=[$DEFAULT_SG],assignPublicIp=ENABLED}" \
                      --force-new-deployment
                    
                    sleep 60
                    continue
                  fi
                fi
              fi
            fi
          fi
          
          sleep 30
        done
        
        if [ $WAIT_ATTEMPTS -eq $MAX_WAIT_ATTEMPTS ]; then
          echo "Service failed to stabilize after $MAX_WAIT_ATTEMPTS attempts"
          
          # Diagnóstico final completo
          echo "=== FINAL DIAGNOSTICS ==="
          aws ecs describe-services --cluster desafio-lacrei-cluster-production --services desafio-lacrei-service-production
          
          TASK_ARNS=$(aws ecs list-tasks --cluster desafio-lacrei-cluster-production --service-name desafio-lacrei-service-production --query 'taskArns[]' --output text)
          if [ ! -z "$TASK_ARNS" ] && [ "$TASK_ARNS" != "None" ]; then
            echo "=== TASK DETAILS ==="
            aws ecs describe-tasks --cluster ${{ secrets.CLUSTER_NAME }} --tasks $TASK_ARNS
            
            # Mostrar logs das tasks que falharam
            for TASK_ARN in $TASK_ARNS; do
              TASK_ID=$(echo $TASK_ARN | cut -d'/' -f3)
              echo "=== LOGS FOR TASK: $TASK_ID ==="
              aws logs get-log-events \
                --log-group-name "/ecs/desafio-lacrei-production" \
                --log-stream-name "ecs/desafio-lacrei-app/$TASK_ID" \
                --query 'events[*].message' --output text || echo "No logs found for task $TASK_ID"
            done
          fi
          exit 1
        fi
        
        echo "Service stabilized successfully!"
    
    - name: Validate production deployment
      run: |
        echo "✅ Production deployment validation - migrations handled by entrypoint script"
        echo "The entrypoint script in the Docker container automatically handles:"
        echo "1. Database connectivity check"
        echo "2. Running migrations"  
        echo "3. Starting Gunicorn server"
        echo ""
        echo "No separate migration task needed - avoiding duplicate migration execution."
    
    - name: Health check
      run: |
        echo "Performing health check via Load Balancer..."
        
        # Obter o DNS do Network Load Balancer
        NLB_DNS=$(aws elbv2 describe-load-balancers --names "desafio-lacrei-nlb-production" --query 'LoadBalancers[0].DNSName' --output text --region ${{ secrets.AWS_REGION }})
        
        if [ ! -z "$NLB_DNS" ] && [ "$NLB_DNS" != "None" ]; then
          echo "Load Balancer DNS: $NLB_DNS"
          
          # Verificar se a aplicação está respondendo corretamente via Load Balancer
          for i in {1..10}; do
            if curl -f "http://${NLB_DNS}/health/" --connect-timeout 10 --max-time 30; then
              echo "✅ Health check passed via Load Balancer"
              break
            else
              echo "⏳ Health check attempt $i/10 - waiting for service to be ready..."
              
              # Verificar status do target group
              TARGET_GROUP_ARN=$(aws elbv2 describe-target-groups --names "desafio-lacrei-tg-v2-production" --query 'TargetGroups[0].TargetGroupArn' --output text --region ${{ secrets.AWS_REGION }})
              if [ ! -z "$TARGET_GROUP_ARN" ]; then
                echo "Checking target health..."
                aws elbv2 describe-target-health --target-group-arn "$TARGET_GROUP_ARN" --region ${{ secrets.AWS_REGION }}
              fi
              
              if [ $i -eq 10 ]; then
                echo "❌ Health check failed after 10 attempts"
                exit 1
              fi
              sleep 30
            fi
          done
        else
          echo "⚠️ Could not retrieve Load Balancer DNS, skipping health check"
        fi

  rollback:
    name: 🔄 Rollback
    runs-on: ubuntu-latest
    if: failure() && (needs.deploy-staging.result == 'failure' || needs.deploy-production.result == 'failure')
    needs: [deploy-staging, deploy-production]
    environment: production
    
    steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}
    
    - name: Rollback to previous version
      run: |
        # Obter a task definition anterior
        CURRENT_TASK_DEF=$(aws ecs describe-services --cluster desafio-lacrei-cluster-production --services desafio-lacrei-service-production --query 'services[0].taskDefinition' --output text)
        CURRENT_REVISION=$(echo $CURRENT_TASK_DEF | cut -d':' -f2)
        PREVIOUS_REVISION=$((CURRENT_REVISION - 1))
        
        # Reverter para a versão anterior
        aws ecs update-service \
          --cluster desafio-lacrei-cluster-production \
          --service desafio-lacrei-service-production \
          --task-definition desafio-lacrei-production:$PREVIOUS_REVISION
        
        # Aguardar estabilização
        aws ecs wait services-stable \
          --cluster desafio-lacrei-cluster-production \
          --services desafio-lacrei-service-production
        
        echo "Rollback completed to revision $PREVIOUS_REVISION"